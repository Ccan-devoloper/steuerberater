name: Fallsammlung inventarisieren

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/fallsammlung-inventar.yml'
      - 'src/data/**'

permissions:
  contents: read

jobs:
  inventar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
      - name: Lernmodule ausgeben
        shell: bash
        run: |
          node <<'NODE'
          const fs = require('fs');
          const path = require('path');
          const dir = 'src/data';
          const files = fs.readdirSync(dir)
            .filter(f => /^module(?:-basis|-vertiefung).*\.js$/.test(f))
            .sort();
          const rows = [];
          const key = name => new RegExp(`["']?${name}["']?\\s*:\\s*`);
          for (const file of files) {
            const text = fs.readFileSync(path.join(dir, file), 'utf8');
            const starts = [...text.matchAll(/["']?id["']?\s*:\s*(\d+)\s*,/g)];
            for (let i = 0; i < starts.length; i++) {
              const start = starts[i].index;
              const end = i + 1 < starts.length ? starts[i + 1].index : text.length;
              const block = text.slice(start, end);
              const id = Number(starts[i][1]);
              const area = block.match(new RegExp(key('area').source + `["']([^"']+)["']`))?.[1] || '';
              const title = block.match(new RegExp(key('title').source + `["']([^"']+)["']`))?.[1] || '';
              const law = block.match(new RegExp(key('law').source + `["']([^"']+)["']`))?.[1] || '';
              if (title) rows.push({ id, area, title, law, file });
            }
          }
          rows.sort((a,b) => a.id-b.id);
          const lernmodule = rows.filter(r => r.area !== 'Fall');
          console.log('MODULINVENTAR_JSON_BEGIN');
          console.log(JSON.stringify(lernmodule, null, 2));
          console.log('MODULINVENTAR_JSON_END');
          console.log(`Anzahl fachliche Lernmodule: ${lernmodule.length}`);
          console.log(`Anzahl vorhandene Fallmodule: ${rows.length - lernmodule.length}`);
          NODE
      - name: Quellfälle ausgeben
        shell: bash
        run: |
          node <<'NODE'
          const base = 'https://raw.githubusercontent.com/Ccan-devoloper/steuernorm/main/faelle';
          const index = await (await fetch(`${base}/index.json`)).json();
          const out = [];
          for (const meta of index.module) {
            const data = await (await fetch(`https://raw.githubusercontent.com/Ccan-devoloper/steuernorm/main/${meta.datei}`)).json();
            for (const fall of data.faelle || []) {
              out.push({
                id: fall.id,
                titel: fall.titel,
                quellmodul_id: meta.id,
                quellmodul: meta.titel,
                fall_seite: fall.quelle?.fall_seite ?? null,
                loesung_seite: fall.quelle?.loesung_seite ?? null
              });
            }
          }
          console.log('QUELLFAELLE_JSON_BEGIN');
          console.log(JSON.stringify(out, null, 2));
          console.log('QUELLFAELLE_JSON_END');
          console.log(`Anzahl Quellfälle: ${out.length}`);
          NODE
