name: Fallsammlung integrieren

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/fallsammlung-inventar.yml'
      - 'src/**'
      - 'tools/**'
      - 'package.json'
      - 'package-lock.json'

permissions:
  contents: read

jobs:
  integrieren:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: agent/fallsammlung-richtig
          fetch-depth: 0
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
      - name: Quelldaten übernehmen und Oberfläche ergänzen
        run: node tools/fallsammlung-integrieren.mjs
      - name: Abhängigkeiten installieren
        run: npm ci
      - name: Fallsammlung prüfen
        run: npm run check:fallsammlung
      - name: Produktionsbuild
        run: npm run build
      - name: Geprüften Endstand bündeln
        shell: bash
        run: |
          set -euo pipefail
          tar -czf /tmp/fallsammlung-endstand.tar.gz \
            src/App.jsx \
            src/index.css \
            src/components/Fallsammlungsfaelle.jsx \
            src/data/module.js \
            src/data/fallsammlung.js \
            src/data/faelle-offen.js \
            tools/pruefen-fallsammlung.mjs \
            package.json
          sha256sum /tmp/fallsammlung-endstand.tar.gz | tee /tmp/fallsammlung-endstand.sha256
      - uses: actions/upload-artifact@v4
        with:
          name: fallsammlung-endstand
          path: |
            /tmp/fallsammlung-endstand.tar.gz
            /tmp/fallsammlung-endstand.sha256
          if-no-files-found: error
