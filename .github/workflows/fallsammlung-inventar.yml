name: Fallsammlung integrieren

on:
  push:
    branches:
      - agent/fallsammlung-richtig
    paths:
      - '.github/workflows/fallsammlung-inventar.yml'
      - 'src/**'
      - 'tools/**'
      - 'package.json'
      - 'package-lock.json'

permissions:
  contents: write

jobs:
  integrieren:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: agent/fallsammlung-richtig
          fetch-depth: 0
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
      - name: Quelldaten übernehmen und Oberfläche ergänzen
        run: node tools/fallsammlung-integrieren.mjs
      - name: Abhängigkeiten installieren
        run: npm ci
      - name: Fallsammlung prüfen
        run: npm run check:fallsammlung
      - name: Produktionsbuild
        run: npm run build
      - name: Geprüften Stand festschreiben
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "Keine Änderungen festzuschreiben."
            exit 0
          fi
          git commit -m "Fallsammlung modulbezogen integrieren"
          git push origin HEAD:agent/fallsammlung-richtig
